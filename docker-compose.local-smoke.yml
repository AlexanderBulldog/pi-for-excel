services:
  pi-for-excel:
    environment:
      # Local rehearsal: pretend the host Caddy serves https://localhost.
      # runtime-config.json then points at https://localhost/llm-gateway/...
      ADDIN_BASE_URL: https://localhost
